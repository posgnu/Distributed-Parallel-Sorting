syntax = "proto3";

package msg;

service Greeter {
  rpc StartSampleRpc (Empty) returns (Samplesres) {}

  rpc PingRpc (Pingreq) returns (Pingres) {}

  rpc metainfoRpc (MetainfoReq) returns (Empty) {}
}

message Pingreq {
    string ip = 1;
}

message Pingres {
    int32 id = 1;
}

message Empty {

}

message Samplesres  {
    repeated string samples = 1;
}

message MetainfoReq {
    repeated string slaves = 1;
    repeated string pivots = 2;
}

